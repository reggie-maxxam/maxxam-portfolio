# Credentials — never commit
.env
*.env.*
.env.local
.env.production
credentials.json
token.json
service-account.json
*_CREDENTIALS*
*_SECRETS*
*_credentials*
*_secrets*

# OS artifacts
.DS_Store
Thumbs.db
desktop.ini
# Internal tooling — not for public distribution
tools/

# Screenshots containing client-identifiable canvas content — website use only
assets/screenshots-local-only/
